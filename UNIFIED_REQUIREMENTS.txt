# --- Core Server & API ---
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
python-multipart==0.0.6
pydantic==2.5.0
python-dotenv==1.0.0

# --- Pipeline & Processing ---
httpx==0.25.1
pillow==10.1.0
pypdf==3.17.1
rapidfuzz==3.5.2

# --- Streamlit Apps (main / main-dev) ---
streamlit>=1.28.0
pymupdf>=1.23.0
PyPDF2>=3.0.0

# --- Stamp Processing (Placeholder) ---
# NOTE: The 'stamp-processing' code was not found in the workspace.
# If you need to run the stamp detector, add its dependencies here.
# likely:
# torch
# ultralytics
# opencv-python-headless
